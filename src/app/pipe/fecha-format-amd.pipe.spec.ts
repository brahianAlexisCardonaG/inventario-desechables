import { FechaFormatAMDPipe } from './fecha-format-amd.pipe';

describe('FechaFormatAMDPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaFormatAMDPipe();
    expect(pipe).toBeTruthy();
  });
});
